# Visual Version Tree (VVT)
## 可视化版本管理工具 - 专为创意工作者设计

> 一个桌面应用，用于可视化管理3D、平面、剪辑、音乐等创意工程文件的版本关系。无需命令行，通过拖拽和图形交互构建版本树。

---

## 🎯 项目概述

**Visual Version Tree (VVT)** 是一款面向创意工作者（设计师、3D艺术家、视频剪辑师、音乐制作人等）的**纯只读可视化版本管理工具**。它不替代 Git，而是为那些通过"另存为"自然产生版本的创意工作流提供直观的组织和追溯能力。

### 核心理念
- **每个工程文件 = 一个节点**（用文件名 + 最后修改时间唯一标识）
- **边 = 工程文件的迭代方向与关系
- **分支 = 从某个节点出发的创作路径**
- **合并 = 多个节点组合成新节点**（代表资产组合或片段拼接）

---

## 📋 核心功能

### 1. 可视化分支树（顶部面板）
- **DAG 图展示**：参考vs code 中git面板，用户可编辑的修改描述**（如"添加角色"、"调整色调"）则在节点下方显示，但是向右生长
- **节点表示**：每个节点代表一个工程文件
  - 选中后显示文件名、最后修改时间
  - 可选显示缩略图/预览图）
- **交互操作**：
  - 拖拽文件到树中 → 拖拽到某分支最后一个节点即为生长；拖拽到某分支中间的节点：创建新节点/分支
  - 生成了新节点后自动跳出编辑框用于给用户输入修改描述**（如"添加角色"、"调整色调"）
  - 选择多个节点 → 右键"合并" → 生成新节点
  - 参考vs code 中git面板布局，无法人工修改
  - 上下左右平移、全屏查看

### 2. 文件追踪面板（底部面板）
- **已追踪文件**：当前项目中已纳入版本管理的工程文件
- **未追踪文件**：项目目录中未被纳入的工程文件
- **手动追踪**：用户通过"选择项目根目录"按钮添加项目
- **文件格式识别**：自动识别常见创意软件工程文件

### 3. 拖拽式版本管理
- **创建分支**：将文件拖入分支树的某个节点 → 生成新分支
- **节点链接**：重命名的文件可手动链接到旧节点（右键"关联历史"）
- **多节点合并**：框选多个节点 → 右键"合并" → 生成新节点并关联文件（代表组合场景/拼接片段）

### 4. 节点预览
- **手动附加预览**：每个节点可附加一张图片（JPG/PNG）或视频（MP4），在选中节点后可操作
- **预览显示**：节点悬停或选中时显示预览
- **预览管理**：可在节点属性面板中上传/替换/删除预览

### 5. 项目隔离
- **独立项目**：每个项目的数据完全隔离
- **不支持跨项目引用**：分支树不能引用其他项目的节点

---

## 🗂️ 支持的文件格式

用户可在项目创建初期指定工程文件扩展名，后续只能添加新的扩展名不能删除。只有指定扩展名的文件可用于版本图的构建，其他文件可作为某节点的预览文件使用

## 💾 数据存储

### 元数据存储位置
- 每个项目根目录下创建隐藏文件夹：`.vmtree/`
具体结构自行设计

## UI要求：
界面上半大部分部是参考vs code 中git面板，但是向右生长的版本控制图，
界面下方是文件显示，点击节点后的详细信息（新建小窗口）

以下是当前已有的python第三方库（非必要不安装新的）：
altgraph                  0.17.5
attrs                     25.4.0
jsonschema                4.19.2
jsonschema-specifications 2025.9.1
networkx                  3.2.1
numpy                     2.2.6
packaging                 25.0
pathlib2                  2.3.7
pefile                    2024.8.26
pillow                    12.0.0
pip                       25.2
pyinstaller               6.17.0
pyinstaller-hooks-contrib 2025.10
PyQt6                     6.10.1
PyQt6-Qt6                 6.10.1
PyQt6_sip                 13.10.3
PySide6                   6.6.0
PySide6-Addons            6.6.0
PySide6-Essentials        6.6.0
pywin32-ctypes            0.2.3
referencing               0.37.0
rpds-py                   0.28.0
setuptools                80.9.0
shiboken6                 6.6.0
six                       1.17.0
typing_extensions         4.15.0
watchdog                  3.0.0
wheel                     0.45.1